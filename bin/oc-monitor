#!/bin/bash
# Open Control - Build, upload and monitor
# Usage: oc-monitor [env]
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../lib/common.sh"

kill_monitors
oc_init "$1"
oc_build
oc_upload
oc_finalize 0
oc_monitor  # Does not return
